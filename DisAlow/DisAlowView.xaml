<Window x:Class="CHD.DisAlowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CHD"
             mc:Ignorable="d" 
             Title="Dis-Alow Join"
             MaxHeight="245" MaxWidth="370"
             Height="245" Width="370"
             WindowStyle="SingleBorderWindow"
             WindowStartupLocation="CenterScreen"
             ResizeMode="NoResize">

    <Grid>

        <GroupBox
    Header="Action"
    BorderBrush="Black"
    HorizontalAlignment="Center"
    VerticalAlignment="Top"
    Height="70"
    Width="350" 
    Margin="0,10,0,0"
        />
        <GroupBox
    Header="Option"
    BorderBrush="Black"
    HorizontalAlignment="Center"
    VerticalAlignment="Top"
    Height="70"
    Width="350" 
    Margin="0,90,0,0"
        />

        <StackPanel     
            Orientation="Horizontal"
            VerticalAlignment="Top"
            HorizontalAlignment="Left"
            Margin="20,40,0,0"
            Height="25"
>
            <RadioButton
            x:Name="rb_dis"
            Content="DisAlow"
            GroupName="g2"
            IsChecked="{Binding IsDisallowSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            />
            <RadioButton
            x:Name="rb_alow"
            Content="Alow"
            Margin="90,0,0,0"
            GroupName="g2"
            IsChecked="{Binding  IsAllowSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            />
        </StackPanel>
        
        <StackPanel     
            Orientation="Horizontal"
            VerticalAlignment="Top"
            HorizontalAlignment="Left"
            Margin="20,120,0,0"
            Height="25"
>
            <RadioButton
            x:Name="rb_wall"
            Content="Start/End Point"
            GroupName="g1"
            IsChecked="{Binding IsTwoSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            />
            <RadioButton
            x:Name="rb_beam"
            Content="Compare With Selected Point"
            Margin="50,0,0,0"
            GroupName="g1"
            IsChecked="{Binding IsOneSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            />
        </StackPanel>

        <StackPanel
         Orientation="Horizontal"
         HorizontalAlignment="Right" 
         VerticalAlignment="Top"
         Margin="0,175,5,0"
         Height="23"
    >
            <Button 
             x:Name="bt_beam"
             Content="Beam" 
             Width="80"  
             Margin="10,0,0,0"
             Command="{Binding BeamCmd}"
             />
            <Button 
             x:Name="bt_wall"
             Content="Wall" 
             Width="80" 
             Margin="15,0,0,0" 
             Command="{Binding WallCmd}"
             />
            <Button 
              x:Name="bt_cancel"
              Content="Cancel" 
              Width="80" 
              Margin="15,0,0,0" 
              Command="{Binding CancelCmd}"
  />
        </StackPanel>

    </Grid>
</Window>
